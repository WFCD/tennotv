<!DOCTYPE html>
<html>
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    {{#if title}}
      <title>Tenno.tv | {{title}}</title>
    {{else}}
      <title>Tenno.tv</title>
    {{/if}}
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta property="og:title" content="Tenno.tv" />
    <meta property="og:description" content="It's YouTube, but less suck." />
    <meta property="og:url" content="https://tenno.tv" />
    <meta property="og:image" content="img/logo.png?v={{lookup sums 'img/logo.png`'}}" />

    {{> analytics}}
    {{> dependencies}}
  </head>
  <body>
    {{> nav}}
    {{> alerts}}
    {{{body}}}
    {{> footer}}
  </body>
</html>
